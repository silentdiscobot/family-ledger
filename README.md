# Family Ledger

家庭记账网站，使用 Flask + SQLite + 原生 HTML/CSS/JavaScript 实现。

## 运行

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

打开 `http://127.0.0.1:5050`。

